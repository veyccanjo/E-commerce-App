<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="searchViewStyle">@style/Widget.SearchView.Custom</item>
    </style>
    <!-- styles.xml -->
    <style name="MySearchViewTheme" parent="ThemeOverlay.AppCompat.Light">
        <!-- Change background -->
        <item name="queryBackground">@android:color/white</item>
        <!-- Change hint and text color (works better with code for full control) -->
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/black</item>
    </style>



</resources>